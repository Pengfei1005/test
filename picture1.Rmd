---
title: "picture1"
output: html_document
---

```{r}
library(pacman)
p_load(ggplot2, gapminder)
```

```{r}
ggplot(data = gapminder, aes(x = gdpPercap, y = lifeExp, size = pop, col = continent)) + 
  geom_point(alpha = 0.3)
```
